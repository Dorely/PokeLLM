{
  "Provider": {
    //"LLM": "OpenAI", // Options: OpenAI, Ollama, Gemini
    "LLM": "Ollama", // Options: OpenAI, Ollama, Gemini
    "Embedding": "Ollama" // Options: OpenAI, Ollama (defaults to LLM provider if omitted)
  },
  "OpenAi": {
    "ApiKey": "", // secret
    //"ModelId": "gpt-4.1-mini", // Primary chat model
    "ModelId": "gpt-5-mini", // Primary chat model
    "EmbeddingModelId": "text-embedding-3-small", // Default embedding model
    "EmbeddingDimensions": 1536
  },
  "Gemini": {
    "ApiKey": "", // secret
    "ModelId": "gemini-2.5-flash" // Default Gemini model
  },
  "Ollama": {
    "Endpoint": "http://localhost:11434", // Ollama endpoint URL
    //"ModelId": "gemma3", // Alternate LLM model - doesn't support tools
    //"ModelId": "deepseek-r1:8b", // Alternate LLM model - doesn't support tools
    //"ModelId": "qwen3", // Alternate LLM model
    //"ModelId": "llama3.1", // Default LLM model
    "ModelId": "gpt-oss", // Default LLM model
    "EmbeddingModelId": "embeddinggemma", // Alternate embedding model
    //"EmbeddingModelId": "nomic-embed-text", // Default embedding model
    "EmbeddingDimensions": 768
  },
  "Qdrant": {
    "Host": "localhost",
    "Port": 6334
  },
  "GameState": {
    "DataDirectory": "GameData" // Relative to the current working directory
  }
}
